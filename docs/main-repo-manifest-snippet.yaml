# Drop this into the main cuvis-ai repo at:
#   configs/plugins/augment.yaml
# AFTER tagging v0.1.0 here and verifying the git-tag manifest model loads cleanly
# (see release-checklist.md "Verify the tagged manifest" section).
plugins:
  augment:
    repo: "https://github.com/cubert-hyperspectral/cuvis-ai-augment.git"
    tag: "v0.1.0"
    provides:
      - cuvis_ai_augment.node.compose.AugmentationCompose
